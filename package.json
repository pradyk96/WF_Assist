{
  "name": "wf-assist",
  "version": "0.2.0",
  "private": true,
  "description": "Embeddable, voice-enabled AI portfolio assistant for Wings Forever.",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "doctor": "node doctor.mjs",
    "check": "node --check server.mjs && node --check wf-assist-widget.js && node --check scene.js && node --check scroll-effects.js && node --check doctor.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
